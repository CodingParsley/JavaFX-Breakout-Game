package gameComponents;

import java.lang.reflect.Method;

import application.TheController;
import javafx.application.Application;
import javafx.scene.layout.Pane;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.stage.Stage;

public class StartScreenView extends Application{
	public Button play;

	public StartScreenView(){
		play = new Button("Play");
		play.setTranslateX(50);
		play.setTranslateY(50);
	}

	@Override
	public void start(Stage primaryStage) throws Exception {
		Pane layout = new Pane();
		layout.getChildren().add(play);
		primaryStage.setScene(new Scene(layout,TheController.getBoardWidth(),TheController.getBoardHeight()-100));
		primaryStage.show();
	}

}
