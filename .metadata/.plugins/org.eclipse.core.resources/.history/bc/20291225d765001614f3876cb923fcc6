package gameComponents;

public class Packet extends Rectangle {
	private double speed;

	protected Packet(Coordinate topLeft, double width, double height, RectangleType type, double speed) {
		super(topLeft, width, height, type);
		this.speed = speed;
	}
	protected Packet(Coordinate topLeft, double width, double height, int id RectangleType type, double speed) {
		super(topLeft, width, height, type);
		this.speed = speed;
	}

	public Packet setSpeed(double speed) {
		return new Packet(this.getTopLeftCoordinate(), this.getWidth(), this.getHeight(), this.getType(), speed);
	}

	public Packet getMove() {
			return new Packet(this.getTopLeftCoordinate().getMoveVelocity(90, speed), this.getWidth(), this.getHeight(), this.getType(),
				  speed);

	}

	public double getSpeed() {
		return speed;
	}

}
